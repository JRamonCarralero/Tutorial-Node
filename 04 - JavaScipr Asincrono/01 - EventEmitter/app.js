/*
    Vamos a utilizar el módulo events, que nos permite crear y gestionar eventos
    Los eventos son funciones que se ejecutan en el momento en el que se dispara el evento
*/

// Importamos el módulo events
const EventEmitter = require('events')   // Nos devuelve la clase EventEmitter

console.log(EventEmitter)
console.log(typeof EventEmitter)
